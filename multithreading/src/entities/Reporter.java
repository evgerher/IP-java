package entities;
//  Отчет (reporter). Интерфейс. Декларирует метод, при вызове которого в консоль выводится «информация о состоянии объекта».

public interface Reporter {
  void describe();
}
